server:
  port: 80
  servlet:
    session:
      timeout: 30m
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
mybatis-plus:
  mapper-locations: xml/*Mapper.xml
  mapper-base-package: bootystar.test.mapper
logging:
  level:
    root: info
    bootystar.test.mapper: debug

